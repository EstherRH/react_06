
// operación condicional ternario

// ejemplo: si el usuario está o no activo

const activo = true;
let mensaje = '';

// ejemplo normala
// if (!activo) {
if (activo) {
    mensaje = 'activo'
} else {
    mensaje = 'inactivo'
}
console.log(mensaje);

// ternario

const mensaje1 = ( activo ) ? 'activo' : 'inactivo'
console.log(mensaje1);







